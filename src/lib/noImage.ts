/** src/lib/noImage.ts — URL placeholder utk konten tanpa gambar. */
import noImage from "../assets/images/no-image.png";

/** Gambar default saat berita/konten tak punya gambar. */
export const NO_IMAGE = noImage;
